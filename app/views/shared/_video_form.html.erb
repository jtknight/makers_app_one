<%= form_for(@video) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :link, placeholder: "Paste link to YouTube or Vimeo video here..." %>
  </div>
  <%= f.submit "Save Video", class: "btn btn-large btn-primary" %>
<% end %>